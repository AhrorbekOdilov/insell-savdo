import 'package:flutter/material.dart';

class PDataTable extends StatelessWidget {
  const PDataTable({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Row(
          children: [],
        ),
        Column(
          children: [
            Row(children: []),
          ],
        )
      ],
    );
  }
}
